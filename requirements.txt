# ==============================================================================
# RealJarvis Dependencies (Cross-Platform PEP-508 Specification)
# Works seamlessly on both Windows and Linux:
#   pip install -r requirements.txt
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Core AI, LLM & Reasoning Engines (Cross-Platform)
# ------------------------------------------------------------------------------
google-genai>=0.1.0        # Google Gemini AI Foundation Brain
anthropic>=0.18.0          # Anthropic Claude AI Fallback Engine
beautifulsoup4==4.12.2     # Web parsing & document scraping
requests>=2.31.0           # HTTP API client
pandas>=2.0.0              # Structured tabular data analysis
openpyxl>=3.1.0            # Excel workbook processing
PyPDF2>=3.0.0              # PDF parsing & text extraction
python-docx>=1.1.0         # Microsoft Word document processor
fpdf2>=2.7.0               # Automated PDF report synthesizer

# ------------------------------------------------------------------------------
# 2. Audio, Speech-to-Text & Text-to-Speech (Cross-Platform)
# ------------------------------------------------------------------------------
SpeechRecognition>=3.10.0   # Audio capture & acoustic recognition
edge-tts>=6.1.0             # Neural Hindi/English cloud TTS
pygame>=2.5.0               # Audio mixer & barge-in interruption pipeline
playsound==1.2.2            # Audio player fallback
sounddevice>=0.4.6          # High-speed mic amplitude & clap sensing
numpy>=1.24.0               # Signal processing & vector mathematics

# ------------------------------------------------------------------------------
# 3. Vision, Hardware Vitals & Computer Control (Cross-Platform)
# ------------------------------------------------------------------------------
psutil>=5.9.0               # CPU, RAM, Battery & Process telemetry
Pillow>=10.0.0              # Image manipulation & UI graphics
opencv-python>=4.8.0        # Computer vision & webcam feed
mediapipe>=0.10.0           # Face mesh & eye tracking
pyautogui>=0.9.54           # Cross-platform mouse & screen automation
selenium==4.15.0            # Browser automation driver
webdriver-manager==4.0.1    # Automated driver binary installer
screen-brightness-control   # Cross-platform monitor brightness control

# ------------------------------------------------------------------------------
# 4. Windows-Only Libraries (Silently SKIPPED on Linux via PEP-508 Markers)
# ------------------------------------------------------------------------------
pywin32>=306; sys_platform == 'win32'             # Windows Native API bindings
pycaw>=20240210; sys_platform == 'win32'          # Windows Core Audio volume control
comtypes>=1.2.0; sys_platform == 'win32'          # COM object interfaces for pycaw
windnd>=1.0.8; sys_platform == 'win32'            # Windows desktop drag-and-drop
pyttsx3>=2.90; sys_platform == 'win32'            # Windows SAPI5 offline voice engine
tkinterdnd2>=0.3.0; sys_platform == 'win32'       # Windows Tkinter DND bindings

# ------------------------------------------------------------------------------
# 5. Linux-Only Libraries (Silently SKIPPED on Windows via PEP-508 Markers)
# ------------------------------------------------------------------------------
python-xlib>=0.33; sys_platform == 'linux'        # Linux X11 display interface
